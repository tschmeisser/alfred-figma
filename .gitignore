# Secrets / local-only
.env
.env.*

# Runtime artifacts created by the workflow
.cache/
.sync.lock
*.tmp

# Built bundle — distributed as a GitHub release, not committed
*.alfredworkflow

# macOS
.DS_Store

# Tooling artifacts
.playwright-cli/
